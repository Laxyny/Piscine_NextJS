# Cahier des charges complémentaire – Extension AI Career Assistant

## Objectif pédagogique (extension)
Cette extension a pour objectif de capitaliser sur le projet Chat Web déjà réalisé par les étudiants afin de :

- réutiliser l’architecture existante (chat, API, base de données)
- transformer un simple chatbot en assistant IA orienté métier (carrière)
- introduire la génération de contenu structuré (CV, lettre)
- comprendre la différence entre conversation libre et génération guidée par formulaire

👉 Aucun nouveau projet n’est créé : le projet Chat Web existant est étendu.

## Contexte pédagogique
Les étudiants disposent déjà :

- d’une application de chat fonctionnelle
- d’une API connectée à une IA
- d’une base SQLite avec Prisma
- d’un frontend React simple

L’objectif est maintenant de faire évoluer cette application vers un AI Career Assistant, sans repartir de zéro.

## Principe de l’extension
L’application doit proposer un nouveau mode d’utilisation, en plus du chat existant :

- un formulaire structuré de profil professionnel
- une génération IA ciblée (CV / lettre / conseils)
- un affichage structuré du contenu généré

Le chat reste présent et fonctionnel.

## Nouvelles fonctionnalités attendues

### 1. Mode « Assistant de carrière »
Ajouter une nouvelle section ou page permettant :

- de saisir :
    - nom (optionnel)
    - formation
    - expériences professionnelles
    - compétences
    - poste ou domaine visé
- de lancer la génération IA via un bouton dédié

### 2. Nouvelle route API
Créer une nouvelle route API :

```
POST /api/career
```

Cette route doit :

- recevoir les données du formulaire
- vérifier que les champs principaux sont remplis
- construire un prompt clair et structuré
- appeler l’API IA
- récupérer une réponse structurée contenant :
    - un CV
    - une lettre de motivation
    - des suggestions
- renvoyer les résultats au frontend

### 3. Génération IA structurée
L’IA doit produire une réponse organisée, par exemple :

- CV (sections : profil, compétences, expériences)
- Lettre de motivation (texte rédigé)
- Suggestions (liste de conseils)

⚠️ Le format exact est libre, mais doit être exploitable facilement côté frontend.

### 4. Base de données (extension)
Ajouter une nouvelle table ou étendre le schéma existant pour stocker :

- les données de profil
- le CV généré
- la lettre générée
- les suggestions
- la date de génération

Le chat existant ne doit pas être cassé.

## Frontend – évolutions attendues
Le frontend doit désormais proposer :

- le chat (existant)
- un formulaire « Assistant de carrière »
- une zone d’affichage claire pour :
    - le CV
    - la lettre
    - les suggestions

L’interface peut rester simple (pas de design complexe attendu).

## Contraintes techniques (inchangées)
- JavaScript uniquement
- Next.js (App Router)
- React côté frontend
- API Routes côté backend
- SQLite + Prisma
- une seule application
- un seul Dockerfile
- pas d’authentification

## Livrables attendus (extension)
En plus des livrables initiaux, les étudiants doivent fournir :

- le code de la nouvelle fonctionnalité
- les modifications du schéma Prisma
- une mise à jour du `README.md` expliquant :
    - le mode Chat
    - le mode Assistant de carrière

## Critères d’évaluation (extension)
L’évaluation portera sur :

- la bonne intégration dans le projet existant
- la clarté du prompt IA
- la séparation frontend / backend
- la qualité du contenu généré
- la non-régression du chat

## Objectif final de l’extension
À la fin de cette extension, les étudiants doivent être capables de :

- faire évoluer une application existante
- adapter une IA à un cas d’usage précis
- structurer des données générées par IA
- comprendre les limites de l’IA générative

Cette extension est pensée comme une montée en compétence naturelle après le projet Chat Web.
